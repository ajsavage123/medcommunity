# Suppress deprecation warnings from optional testing dependencies
# These warnings are from jsdom's dependencies (whatwg-encoding, domexception, abab)
# They don't affect production and jsdom is only used for testing
legacy-peer-deps=false
audit-level=moderate
